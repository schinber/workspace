from typing import List


class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        lth = len(nums)
        for i in range(lth):
            for j in range()


if __name__ == '__main__':
    nums = [1, 2, 3]
